email = input("What is your email?, ").strip()

#Strip removes any whitespace fromt the User 
#Input prompts user 

#Checking for @ sign with valid 
#Invalid if not 
#returning two parts split at username and domain 
username, domain = email.split("@")

if username and ".edu" in domain: 
....print("Valid")
else: 
....print("Invalid")
